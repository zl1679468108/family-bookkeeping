import { AUTH_NAV_LOGIN } from '../../../utils/authCopy'

export default definePageConfig({
  navigationBarTitleText: AUTH_NAV_LOGIN,
  navigationBarBackgroundColor: "#FAF8F5",
  navigationBarTextStyle: "black",
});
