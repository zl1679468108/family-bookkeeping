import { NAV_CATEGORIES } from '../../utils/navCopy'

export default definePageConfig({
  navigationBarTitleText: NAV_CATEGORIES,
  navigationBarBackgroundColor: "#FFFFFF",
  navigationBarTextStyle: "black",
  usingComponents: {},
});
