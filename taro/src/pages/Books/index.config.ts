import { NAV_BOOKS } from '../../utils/navCopy'

export default definePageConfig({
  navigationBarTitleText: NAV_BOOKS,
  navigationBarBackgroundColor: "#FFFFFF",
  navigationBarTextStyle: "black",
  usingComponents: {},
});
