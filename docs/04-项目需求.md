## 产品需求池（待评审）

> 以下为基于当前已完成功能的后续迭代需求。标注 P0/P1/P2 为建议优先级，请审阅后确认。

---

### P0 — 核心体验（建议下个迭代做）

| 编号 | 需求 | 说明 | 涉及层 |
|------|------|------|--------|
确定要做（已验收）    | P0-1 | 交易编辑/删除确认优化 | 编辑页目前缺失独立路由，删除无二次确认。需要：编辑跳转 AddTransaction?edit=id 复用页面，删除前弹出确认 | 前端 |
确定要做（已验收）    | P0-2 | 分类管理（自定义分类） | 目前分类固定 10+6 个，用户无法新增/修改。需要：分类管理页（增删改）、自定义分类持久化到数据库、与现有字典兼容 | 前后端 |
确定要做（已验收）    | P0-3 | 报表导出适配当前时间范围 | 导出 Excel/PDF 目前是全量数据，需适配 Reports 页当前选中的时间范围 | 前后端 |

---

### P1 — 功能增强（建议接下来做）

| 编号 | 需求 | 说明 | 涉及层 |
|------|------|------|--------|
（已验收）| P1-1 | 收入分析 Tab | Reports 页增加「支出分析」「收入分析」两个 Tab，收入侧展示收入来源饼图 + 趋势 | 前端 |
（已验收）| P1-2 | 饼图点击下钻 | 点击饼图某个分类 → 跳转 /transactions?category=food&startDate=... 自动筛选 | 前端 |
（已验收）| P1-3 | 仪表板数据连接 | Dashboard 目前数据为实时请求但都是全量数据，建议改为调用 statistics API 统一口径 | 前后端 |
不做| P1-4 | 仪表板近期趋势小图 | Dashboard 增加最近 3 个月迷你折线图卡片，快速感知趋势 | 前端 |
（已验收）| P1-5 | 预算管理 | budgets 表已建但无前后端实现。用户可设置月度分类预算，超过时预警 | 前后端 |
（已验收）| P1-6 | 搜索功能增强 | Transactions 页目前只有分类/类型筛选，增加关键词搜索（描述字段模糊匹配） | 前后端 |
（已验收）| P1-7 | 暗色模式 | 目前 CSS 变量体系已支持主题化（--fg/--surface 等），增加暗色主题切换 | 前端 |

---

### P2 — 体验优化 & 扩展（未来可做）

| 编号 | 需求 | 说明 | 涉及层 |
|------|------|------|--------|
| P2-1 | 年度对比视图 | Reports 页增加「本年 vs 去年」同时间段对比柱状图 | 前后端 |
| P2-2 | 自定义时间范围 | DateRangeFilter 增加自由起止日期选择器（datepicker） | 前端 |
不做| P2-3 | 交易导入 | 支持 CSV/银行账单导入，自动解析并匹配分类 | 前后端 |
不做| P2-4 | 周期交易/自动记账 | 固定支出（房租/订阅）自动生成交易记录，支持按月/周/年重复 | 前后端 |
| P2-5 | 多账本/家庭共享 | 一个家庭多人共用一个账本，按成员维度拆分统计 | 前后端 |
不做| P2-6 | 移动端 PWA | 添加 Service Worker，支持离线访问和桌面快捷方式 | 前端 |
不做| P2-7 | 通知提醒 | 预算超支提醒、账单到期提醒（邮件/浏览器通知） | 前后端 |
不做| P2-8 | 数据备份 | 支持手动/定时导出全量数据备份到云端 | 后端 |

---

---

---

## 地图功能模块（2026-05-28 评审通过）

> 基于高德地图 JS API 2.0，详细需求见 `docs/05-地图功能需求.md`

### P0 — 本迭代交付

| 编号 | 需求 | 说明 | 涉及层 |
|------|------|------|--------|
| ✅ P0-1 | 数据库扩展 | transactions 表加 latitude/longitude/location_name/poi_id 四个可选字段 + 索引 | 后端 |
| ✅ P0-2 | 记账页集成位置选择 | /add 页面弹窗模式选位置，支持拖拽定位+搜索地址+点击选点 | 前端 |
| ✅ P0-3 | 消费点标记 | /map 页面按分类颜色标记所有带位置交易，点击弹信息窗 | 前端 |
| ✅ P0-4 | 地图筛选栏 | 时间范围+分类多选+金额范围三维筛选，实时联动 | 前端 |
| ✅ P0-5 | 热力图图层 | 高德 HeatMap，标记/热力图双图层切换，< 5 个点自动隐藏 | 前端 |
| ✅ P0-6 | 商户地图视图 | 按 poi_id 聚合商户，圆点大小=消费金额，点击看商户卡片 | 前端 |
| ✅ P0-7 | 商户列表视图 | 地图/列表切换，按金额/次数排序 | 前端 |
| ✅ P0-8 | 附近商户搜索 | 高德 PlaceSearch + 已有消费交叉标记 | 前端 |
| ✅ P0-9 | 导航入口 | Sidebar 新增「消费地图」→ /map | 前端 |
| ✅ P0-10 | 后端地图 API | GET /map/transactions + GET /map/merchants | 后端 |

### P1 — 下个迭代

| 编号 | 需求 | 说明 | 涉及层 |
|------|------|------|--------|
| ⏳ P1-1 | 家庭位置共享 | 成员在地图上显示实时位置 | 前后端 |
| ⏳ P1-2 | 成员消费地图 | 按成员维度拆分消费标记 | 前后端 |
| ⏳ P1-3 | 资产管理-标记 | 标记房产/车位等固定资产 | 前后端 |
| ⏳ P1-4 | 资产管理-关联 | 资产关联相关支出 | 前后端 |
| ⏳ P1-5 | assets 表 | 新增 assets 数据表 | 后端 |
| ⏳ P1-6 | 交易-资产关联 | transactions 新增 asset_id | 后端 |

### P2 — 远期

| 编号 | 需求 | 说明 | 涉及层 |
|------|------|------|--------|
| 🔮 P2-1 | 出行路线记录 | 路径规划 API，记录起终点 | 前后端 |
| 🔮 P2-2 | 费用自动计算 | 路线距离+油耗自动算费用 | 前后端 |
| 🔮 P2-3 | 行程消费绑定 | 行程关联多笔消费 | 前后端 |
| 🔮 P2-4 | trips 表 | 新增 trips 数据表 | 后端 |

### 评审确认（2026-05-28）

| 问题 | 决策 |
|------|------|
| Q1 账本隔离 | ✅ P0 按当前选中账本过滤 |
| Q2 热力图阈值 | ✅ < 5 个点自动隐藏 |
| Q3 商户聚合精度 | ✅ 按高德 poi_id 精确聚合 |
| Q4 位置隐私 | ✅ P0 不做，仅自己可见 |
| Q5 API Key 管理 | ✅ 前端 .env REACT_APP_AMAP_KEY |
| Q6 位置选择组件 | ✅ 弹窗/抽屉模式 |
