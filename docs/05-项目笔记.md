// 粘贴到 Chrome DevTools Console（F12 → Console）可查看token =》数据库token_hash
await crypto.subtle.digest('SHA-256', new TextEncoder().encode('你的token值'))
  .then(buf => Array.from(new Uint8Array(buf))
    .map(b => b.toString(16).padStart(2, '0')).join(''))