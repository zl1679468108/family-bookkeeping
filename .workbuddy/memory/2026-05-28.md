# 2026-05-28 工作日志

## 规范更新
- 在 MEMORY.md 中新增「执行规范」章节，包含：
  - docs/ 文档同步清单（9 个文件）
  - database-init.sql 数据库变更同步规则（7 类结构变更触发提醒）
  - 数据库变更日志表（含 2026-05-26 初始记录）

## P1 功能增强交付
- 完成了 6 个 P1 需求的完整实现（跳过 P1-4 仪表板趋势小图）
- **新增后端模块**：BudgetsModule (5 文件)，4 个 API 端点（GET/PUT budgets, GET /status, POST /copy）
- **后端修改**：Transactions 搜索扩展（controller + service 增加 search ilike）
- **前端新增**：预算管理页面 (Budgets.tsx/Budgets.scss)、暗色模式 (ThemeContext/ThemeProvider/ThemeToggle)、类型定义 (budget.ts)、API 封装 (budgetsApi.ts)
- **前端修改 11 个文件**：Dashboard (statistics API + 预算卡片 + 预警)、Reports (收入分析 Tab + 饼图下钻)、Transactions (URL 参数初始化 + 搜索)、FilterBar (搜索框 + debounce)、ChartCard (onCategoryClick + 暗色适配)、Sidebar (预算菜单 + 主题切换)、路由 (新增 /budgets)、App.tsx (ThemeProvider)、globals.css ([data-theme="dark"])
- **QA**：全部通过 (后端编译 + 前端 tsc --noEmit + 代码审查)
- 产出文档：docs/05-P1-PRD.md、docs/06-P1-Architecture.md

## Bug 修复
- **登录页显示侧边栏**：App.tsx 中 AUTH_ROUTES 未做特判，导致 `/login` 等公开路由在有残留 token 时仍渲染 Sidebar。修复：新增 `isAuthPage` 判断，公开路由直接展示全屏页面不渲染侧边栏。

## 14:41 - 聚焦容器功能（Spotlight）

**需求**：概览页预算超支提醒点击后跳转到预算页面，自动滚动并高亮对应分类行。

**实现**：
1. 创建 `frontend/src/hooks/useFocusItem.ts` — 通用 Hook
   - 从 URL 中读取 `?focus=xxx` 参数
   - 用 `requestAnimationFrame` 等待 DOM 渲染完成后 `scrollIntoView({ behavior: 'smooth', block: 'center' })`
   - 添加 `spotlight--focused` class 触发高亮动画
   - 2.5s 后自动移除高亮并清除 URL 参数

2. 全局 CSS（`globals.css`）添加 spotlight 动画
   - `spotlightPulse`: box-shadow 脉冲（2px→3px + 光晕渐大）
   - `spotlightBorderGlow`: border 颜色呼吸
   - 深色模式适配（`[data-theme="dark"]` 下有独立动画）

3. 修改 `Budgets.tsx`
   - 集成 `useFocusItem({ getTargetSelector: (id) => \`[data-focus="${id}"]\` })`
   - 每行添加 `data-focus={catKey}` 属性
   - 匹配时 className 追加 `spotlight--focused`

4. 修改 `Dashboard.tsx`
   - 左侧"管理预算"按钮：`navigate('/budgets?focus=xxx')`
   - 右侧"查看预算详情"按钮：同上
   - 左侧超支标签 span：可点击跳转
   - 右侧预警列表 div：每个条目可点击跳转
   - 全部 hover 时有 opacity 反馈

**用户交互流**：Dashboard 点击「餐饮超支」→ `/budgets?focus=food_delivery` → 自动滚动到餐饮行（居中）+ 蓝色脉冲高亮 2.5s

**QA**：tsc --noEmit 零错误通过

## 15:07 - useFocusItem API 简化

- 将 `getTargetSelector` 从必填改为可选，默认使用 `data-focus` 属性
- 新增 `dataAttribute` 选项自定义匹配属性名
- Budgets.tsx 调用从 `useFocusItem({ getTargetSelector: (id) => ... })` 简化为 `useFocusItem()`

## 15:18 - Dashboard 预警去重

- 将左右两个独立卡片合并为单一「预算概览」卡片
- 预警详情（金额/百分比）嵌入卡片内部展示
- 删除右侧独立预警横幅，避免重复显示相同信息
- 清理未使用的 `getProgressColor` 函数

## 15:20 - P2 需求实现

### P2-2 自定义时间范围 ✅
- DateRangeFilter 新增「自定义」按钮，点击展开起止日期选择器
- 新增「上月」预设选项
- 自定义区间支持 `type="date"` 原生日期选择器

### P2-1 年度对比视图 ✅
- 后端新增 `GET /api/statistics/yoy-comparison` API
  - DTO: year + type
  - Service: 按 1-12 月聚合本年 vs 去年数据
- 前端 ChartCard 扩展 'yoy' 柱状图类型
  - 双柱并排：今年(蓝) vs 去年(灰)
  - 暗色模式适配
- Reports 页在趋势+饼图下方新增全宽年度对比图表

### P2-5 多账本/家庭共享 ✅（核心架构）
**数据库变更** ⚠️：
- books 表（id, name, owner_id, created_at, updated_at）
- book_members 表（id, book_id, user_id, role, joined_at）
- transactions 表新增 book_id UUID（可空，向后兼容）
- budgets 表新增 book_id UUID（可空，向后兼容）

**后端**：
- 新增 BooksModule（books.controller/service/module + dto/book.dto + book-id.decorator）
- 6 个 API 端点：列表/详情/创建/删除/成员列表/邀请/退出
- 修改 Transaction/Budgets/Statistics 服务支持 book_id 过滤
- 新增 `@BookId()` 装饰器读取 `x-book-id` header

**前端**：
- 新增 `hooks/useBook.tsx`：BookContext + BookProvider + localStorage 持久化
- 新增 `services/booksApi.ts`：6 个 API 函数
- 新增 `components/BookSwitcher`：侧边栏下拉账本切换器
- 新增 `pages/Books.tsx`：账本管理页（创建/删除/邀请/退出/成员列表）
- 修改 `api.ts`：自动在 authenticated 请求中附加 `x-book-id` header
- 修改 `App.tsx`：包裹 BookProvider
- 修改 `Sidebar`：在用户信息下方嵌入 BookSwitcher
- 切换账本时刷新所有缓存（transactions/statistics/budgets）

**兼容策略**：book_id 为 NULL 时视为"默认账本"，不影响旧数据。
⚠️ 数据库变更提醒：新增 2 张表 + 2 列 + 5 个索引 - 请执行 docs/database-init.sql

### 跳过（标注不做）
P2-3 交易导入 | P2-4 周期交易 | P2-6 PWA | P2-7 通知提醒 | P2-8 数据备份

## 15:58 - 账本功能优化（3 项改进）

**1. 默认账本加入列表**
- BooksPage 新增 renderDefaultBook() 卡片，始终显示在列表顶部
- 标记「系统默认」标签，不可删除/重命名

**2. ConfirmDialog 升级为标准删除组件**
- 从 Tailwind 改为 CSS 变量（var(--surface)/var(--fg)/var(--muted) 等），暗色模式自动适配
- BooksPage 删除/退出操作统一使用 ConfirmDialog，不再用 window.confirm
- 规范：以后项目所有删除二次确认都使用此组件

**3. 邀请简化 + 重命名**
- 邀请改为纯查用户存在性：输入邮箱 → 用户存在就添加，不存在就提示错误（不发邮件）
- 后端新增 PUT /api/books/:id 重命名接口
- 前端支持行内重命名：点击名称 → 输入框 → Enter/保存按钮确认

**QA**：前后端 tsc --noEmit 零错误通过

## 15:20 - 地图功能模块需求讨论 & PRD

**需求来源**：用户希望围绕地图增加记账可视化功能。

**讨论结论**：
- 地图 API：高德地图 JS API 2.0 (免费额度 30万次/天)
- React 封装：@uiw/react-amap
- 位置录入：手动选择（记账时弹窗选点）
- 功能方向 A~E，P0 做 A(消费足迹热力图) + C(商户地图)

**产出**：
- `docs/05-地图功能需求.md`：完整 PRD（产品目标、5 个用户故事、P0 10项/P1 6项/P2 4项、UI 设计、数据模型变更、技术选型）
- 更新 `docs/04-项目需求.md`：新增「地图功能模块」章节，Q1-Q6 全部确认
- Q1-Q6 评审全部通过（账本隔离/热力图阈值<5点/poi_id聚合/隐私P0不做/前端env key/弹窗选位置）

**数据模型变更**（⚠️ 待执行）：
- transactions 表新增 4 个可选字段：latitude(DECIMAL 10,7), longitude(DECIMAL 10,7), location_name(VARCHAR 200), poi_id(VARCHAR 100)
- 部分索引 idx_transactions_location

**P0 10 项需求已验收，等待开工**

## 16:47 - 地图功能架构设计

**产出**：`docs/06-地图功能-架构设计.md`

**关键决策**：
- 后端：新建独立 MapModule（backend/src/map/，4 文件），不耦合现有 Transaction 模块
- 前端：新建 /map 页面 + 5 个子组件（MapHeader, MapCanvas, MerchantCard, MerchantList, LocationPicker）
- 数据流：react-query → mapApi.ts → GET /api/map/transactions + /api/map/merchants
- POI 搜索：前端直接调高德 JS API PlaceSearch，不经后端
- book_id 隔离：复用现有 api.ts x-book-id 拦截器，无需额外处理
- DTO 扩展：TransactionModule 的 Create/Update DTO 新增 4 个可选位置字段

**任务拆解 10 项**，分 3 批执行：
- 第一批 (T1+T2+T3)：数据库 + 后端 API + 前端入口
- 第二批 (T4+T5)：消费点标记 + 记账位置选择
- 第三批 (T6~T10)：热力图 + 筛选 + 商户视图 + POI搜索

**数据库变更** ⚠️：transactions 表新增 4 个字段 + 部分索引 - 需执行 SQL

## 16:58 - 第一批交付：数据库 + 后端 API + 前端入口

**T1 数据库扩展** ✅：
- database-init.sql 新增：transactions 表 4 个位置字段（latitude/longitude/location_name/poi_id）+ 部分索引 idx_transactions_location
- 使用 DO $$ 块兼容重复执行
- ⚠️ 需用户在生产环境执行 SQL

**T2 后端 MapModule** ✅：
- 新建 backend/src/map/（4 文件）
  - map.module.ts：import SupabaseModule + AuthModule
  - map.controller.ts：GET /api/map/transactions + GET /api/map/merchants，TokenAuthGuard + CurrentUser + BookId
  - map.service.ts：getTransactionsWithLocation（按条件筛选）+ getMerchantSummary（应用层分组聚合）
  - dto/map-query.dto.ts：MapTransactionsQueryDto + MerchantQueryDto（class-validator）
- 修改 app.module.ts：注册 MapModule
- 修改 transaction.service.ts：Transaction 接口新增 4 个可选位置字段（兼容 create/update 透传）
- 后端 tsc --noEmit 零错误

**T3 前端入口** ✅：
- 新建 frontend/src/pages/Map.tsx（占位页面）
- 修改 routes/routes.tsx：新增 /map 路由（lazy import + isPrivate）
- 修改 Sidebar/index.tsx：新增「消费地图」导航项 + getActiveItemId 映射
- 前端 tsc --noEmit 零错误

## 17:00 - 第二批交付：消费点标记 + 记账位置选择

**依赖安装**：
- @uiw/react-amap v7.1.15（高德地图 React 封装）
- @amap/amap-jsapi-types（AMap TypeScript 类型声明）

**T4 消费点标记** ✅：
- 新建 types/map.ts：MapTransaction / MerchantSummary / MapFilters / LocationResult
- 新建 services/mapApi.ts：fetchMapTransactions + fetchMerchantSummary
- 新建 utils/categoryColors.ts：16 个分类颜色映射 + getCategoryColor / getTypeColor
- 新建 components/MapCanvas/index.tsx + index.scss：
  - 高德地图 + Marker 标记（按分类颜色 + 金额大小）
  - InfoWindow 弹窗（分类图标/金额/地址/备注/日期）
  - 加载态（spinner）、空态（引导文案）、地图自适应居中
- 更新 pages/Map.tsx：DateRangeFilter 筛选 + react-query 数据获取 + 统计摘要
- @uiw/react-amap v7 类型与运行时存在差异，使用 as any 桥接（npm issue）

**T5 记账位置选择** ✅：
- 新建 components/LocationPicker/index.tsx + index.scss：
  - 弹窗模式，不破坏记账流程
  - 高德地图点击选点 + 逆地理编码（Geocoder）
  - 地址搜索（PlaceSearch）+ Enter 键触发
  - 选中地址展示 + 清除/确认按钮
- 修改 pages/AddTransaction.tsx：
  - 新增 location state + LocationPicker 集成
  - 表单「添加位置」按钮 + 已选位置展示
  - 提交时附带 latitude/longitude/locationName/poiId
  - 编辑模式自动回填已有位置
- 新增 types/amap.d.ts + globals.css 地图样式

**QA**：前后端 tsc --noEmit 零错误

**⚠️ 待办**：
- 需申请高德 API Key，配置到 frontend/.env.development 的 REACT_APP_AMAP_KEY
- 需执行 database-init.sql 迁移 SQL

## 17:15 - 第三批交付：热力图 + 筛选 + 商户视图 + POI 搜索

**API Key 配置** ✅：
- .env.development + .env.production 已配置 REACT_APP_AMAP_KEY

**T6 热力图** ✅：
- MapCanvas 增加 viewMode='heatmap'
- 使用 AMap.HeatMap 插件，加载/清除热力图层
- 颜色渐变：绿→黄→红→紫
- <5 个点时按钮 disabled

**T7 筛选栏** ✅：
- Map.tsx 新增分类多选 chip（复用 useCategories 数据）
- 类型筛选（全部/支出/收入）
- 与 URL、react-query 联动实时刷新

**T8 商户地图视图** ✅：
- viewMode='merchant-map'：按 poi_id/location_name 聚合商户
- 圆点大小 = 消费金额占比（16px~48px 动态缩放）
- 点击弹出商户卡片（名称/总金额/次数/最近日期）

**T9 商户列表视图** ✅：
- 新建 components/MerchantList/（index.tsx + index.scss）
- 按金额/次数排序切换
- 进度条可视化（金额红色渐变、次数蓝绿渐变）
- 每行显示：排名/商户名/金额进度条/次数进度条/最近日期

**T10 POI 搜索** ✅：
- MapCanvas 左上角搜索框覆盖层
- 调用高德 PlaceSearch API，展示搜索结果列表
- 与已有消费数据交叉标记（✅有记录 / 📍无记录）
- 点击搜索结果地图定位到对应位置

**修改文件**：
- pages/Map.tsx：完全重写（筛选栏+视图切换+商户列表条件渲染）
- components/MapCanvas/index.tsx + index.scss：完全重写（热力+商户+搜索）
- components/MerchantList/index.tsx + index.scss：新建
- styles/globals.css：新增 map-chip/map-toolbar-row/map-view-toggle 等样式

**QA**：前端 tsc --noEmit 零错误

**⚠️ 仍需执行**：database-init.sql 迁移 SQL

## 17:25 - Bug 修复：高德插件加载

**问题**：高德 JS API 2.0 的 Geocoder/PlaceSearch/HeatMap 是插件，直接 `new AMap.Geocoder()` 报错 `is not a constructor`

**修复**：
- LocationPicker：useEffect 中用 `AMap.plugin(['AMap.Geocoder', 'AMap.PlaceSearch'], callback)` 加载插件
- MapCanvas：同样在 useEffect 中用 `AMap.plugin(['AMap.PlaceSearch', 'AMap.HeatMap'], callback)` 加载
- 新增 `pluginsReady` 状态控制交互启用时机
- 搜索按钮、输入框在插件未就绪时显示加载态
- QA：tsc --noEmit 零错误

## P0 地图功能模块 — 全部交付 ✅

| 批次 | 任务 | 状态 |
|------|------|:--:|
| 第一批 | T1 数据库 / T2 后端API / T3 前端入口 | ✅ |
| 第二批 | T4 消费点标记 / T5 记账位置选择 | ✅ |
| 第三批 | T6 热力图 / T7 筛选 / T8 商户地图 / T9 商户列表 / T10 POI搜索 | ✅ |
