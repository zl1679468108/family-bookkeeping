# 2026-06-07 工作日志

## Taro 端 UI 重设计

- 用户对现有 Taro 小程序 UI 不满意（色调/字体/颜色都需要重来）
- **设计方向确定**：从「珊瑚橙热情」转向「安静鼠尾草绿」— 安静记账
- **核心变更**：
  - 主色 #E07B4C → #5B9A7A（鼠尾草绿 Sage）
  - 支出色 #E07B4C → #5B7FA5（石板蓝 Slate）
  - 背景 #FAF8F5 → #F5F2ED
  - 文字色 #2C2416 → #1A1A1A
  - 去除所有渐变头部、漂浮光球动画、多层阴影、3D text-shadow
  - 卡片统一使用 0.5px 细边框替代 box-shadow
  - iOS 风格分段控件替代原支出/收入切换
- **产出**：完整设计规范文档 `taro/design-spec.html`
  - 包含：色彩系统、字体排版、间距布局、9 个组件、11 个页面、4 种状态、10 条优化建议、CSS 变量速查表
- **待定**：用户确认设计稿后开始改代码

## 设计规范 v2.1 修订（对齐 PC 功能）

- 用户反馈 9 点意见，已全部纳入修订版 `taro/design-spec-v2.1.html`
- **关键修订**：
  1. 支出色 #5B7FA5→#D4785C（Terracotta 暖色），与收入绿形成冷暖强对比
  2. EmptyState 统一公共组件（Props: icon/title/description/actionText/onAction/mode）
  3. NumberPad 功能键加大到 24px/700 字重
  4. 统计页重做：去掉总支出/总收入卡片 → 对齐 PC 三Tab（支出分析/收入分析/成员对比）+ 趋势图 + 分类占比环形图
  5. 所有主要按钮统一 block（占满整行）
  6. 记一笔新增地图选点（高德 SDK，环境变量 TARO_APP_AMAP_KEY/SECRET 从 PC 拷贝）
  7. 分类管理对齐 PC：新增/编辑(名称+emoji)/删除/拖拽排序/默认分类保护
  8. 账本管理对齐 PC：创建/重命名/删除/成员查看/邮箱邀请/退出账本
  9. 公共组件清单：EmptyState、Skeleton、Toast、ConfirmDialog、TransactionItem、SegmentedControl、ProgressBar
  10. 10条优化建议全部纳入实现（夜间模式、触觉反馈、下拉刷新、金额格式化、搜索增强、页面过渡、样式抽离、分类图标统一、首页快捷操作、性能优化）
- 产出 20 项实现清单（P0 共 14 项，P1 共 4 项，P2 共 2 项）

## 设计规范 v3.0 — 完整交互设计稿

- 用户要求每个页面每步点击的交互图都要出现，完完整整从界面到点击
- **先完整梳理了 PC 端 8 个模块的全部交互**：Dashboard (8个交互点)、Transactions (11个)、Reports (7个)、Budgets (6个)、Categories (7个)、Books (7个)、Templates+Calendar、AddTransaction+Map (15个)
- **产出**：`taro/design-spec-v3.html`
  - 17 个章节：色彩定稿 + 6 个公共组件 Props + 环境变量 + 15 个页面/模块完整交互流
  - 每个模块：左侧手机框设计稿 + 右侧逐条交互流（触发方式→状态变化→API调用）
  - 覆盖全部点击场景：账本切换、预算点击跳转、交易左滑编辑删除、搜索防抖、类型/分类筛选链、排序三态循环、分类扇形点击导航、拖拽排序全流程、成员展开/邀请/退出、地图选点全流程等
- **24 项实现清单**（P0×16 + P1×5 + P2×3）

## Taro 开发规范补充（已写入 MEMORY.md）

用户提出三条开工前规范：

1. **功能对齐**：小程序以移动端交互形式一个不差实现 PC 全部功能，开发前先过 PC 代码
2. **代码组织**：公共组件 → components/，模块专属 → pages/模块/components/，单文件 ≤300行(tsx)/≤200行(scss)，样式分全局+模块
3. **Skeleton/EmptyState**：公共组件但支持各模块不同形态（type/count/title/description 等 props 定制）
4. 同时更新：导航栏改白底简洁风格、旧"优先 inline styles"改为"优先 SCSS class"、WXSS 编号改为 #10、代码风格改为 #11

## v3.0 开发实施（17:28 开始）

- **已完成**：
  - #1 app.scss 完全重写（v3.0 设计系统：Sage+Terracotta 色板、卡片边框替代阴影、block 按钮、Segmented/Progress/标签等新工具类）
  - #1 app.config.ts 更新（导航栏白色、TabBar 色改为 Sage）
  - #1 .env.development 新增 AMAP_KEY/SECRET
  - #2-6 SegmentedControl、ProgressBar、TransactionItem 公共组件完成
  - #8 首页 Home 重做（白底导航+结余卡片+预算进度+近期流水+空状态）
  - #9 流水 Transactions 重做（白底导航+搜索+月份/类型/分类筛选+日期分组+左滑删除+排序+分页）
- **Agent已完成**：
  - ✅ EmptyState / Skeleton / Toast 公共组件（Props 升级：mode空/错误，type卡片/列表/图表）
  - ✅ TabBar 重设计（进行中）
  - ✅ AddTransaction（SegmentedControl+分类网格+block保存+模板入口）253行
  - ✅ Profile（用户卡片+菜单列表+退出block）101行
  - ✅ Login / Register / ForgotPassword（block按钮+新色系）
  - ✅ Statistics（三Tab+分类占比可点击导航+月度趋势+成员对比）172行
  - ✅ Categories（新增/编辑(emoji网格)/删除/排序/默认保护）205行
  - ✅ Books（创建/切换/重命名/删除/成员/邀请/退出）253行
  - ✅ Budgets（月份选择+复制上月+编辑金额+保存+进度条）197行
- **P0 收尾**：
  - ✅ LocationPicker 地图选点组件（Map组件+逆地理编码+POI搜索+GPS定位）
  - ✅ .env.development 新增 TARO_APP_AMAP_KEY/SECRET
- **P1/P2 收尾**：
  - ✅ Templates 模板管理页（创建/编辑/删除表单，全 block 按钮）143行
  - ✅ Calendar 日历页（Picker月份导航+日期格子+点击展开交易明细+4卡片月度统计）200行
  - ✅ 夜间模式（app.scss @media prefers-color-scheme:dark 全变量覆盖）
  - ✅ 下拉刷新（app.config.ts window.enablePullDownRefresh:true）
  - ✅ 页面过渡动画（fadeIn 0.2s）
  - ✅ 图片懒加载（全局 config）
- **总计完成**：1个全局SCSS + 6个公共组件 + TabBar + 13个页面 + LocationPicker + 5项全局增强

## UI 布局修复（用户反馈 @images 1-8）

用户指出三个问题：

1. **Home 页 MonthPicker 遮挡**：MonthPicker 在自定义导航栏中位置不合理
   - 修复：MonthPicker 移到内容区 balance card 上方，导航栏仅显示"家庭记账"标题

2. **二级页面重复导航栏**：Budgets/Categories/Books/TemplateManager/Calendar 未设置 `navigationStyle: 'custom'`，系统已有默认导航栏，代码中又叠加自定义 .nav-header，导致双层导航 + 内容被遮挡
   - 修复：全部 5 个页面移除自定义 nav-header 代码和 statusBarH 变量
   - 各页面 nav 中的功能按钮移动到内容区：
     - Budgets: "复制上月" 移到月份选择器右侧
     - Categories: "编辑排序/完成" 移到 SegmentedControl 右侧
     - Books: 无额外按钮
     - TemplateManager: "＋ 新建" 移到列表上方
     - Calendar: 月份 Picker + "今天" 按钮移到内容区工具栏
   - 同步修复 Register/ForgotPassword 的相同问题

3. **Safe area 偏移**：去掉自定义 nav 后，系统导航栏自动处理 safe area，不再需要手动 statusBarHeight padding

4. **编译验证**：`taro build --type weapp` 编译通过，0 error

## 三页面 UI 精修（用户反馈 @3 images）

用户反馈三个问题：

1. **AddTransaction 整体偏上** — custom nav 未处理 safe area，内容被状态栏遮挡；"模板"按钮路由错误
   - 修复：`.nav-header` 添加 `paddingTop: ${statusBarH}px`
   - 修复路由：`/pages/Templates/index` → `/pages/TemplateManager/index`

2. **Statistics 选择日期被挡住** — MonthPicker 在导航栏右侧，与标题挤在一起
   - 修复：MonthPicker 移到内容区 tabs 下方，导航栏仅保留"统计"标题

3. **Profile 信息挤+版本号丑** — 用户卡片布局拥挤，版本号样式简陋；"模板管理"和"消费地图"路由错误
   - 修复：添加 `statusBarH` safe area
   - 用户卡片重构：`user-card-inner` 结构，头像加大到 112rpx，用户名加大到 `--font-xl` 700字重，邮箱用 `--font-sm` 灰色
   - 修复路由：`/pages/Templates/index` → `/pages/TemplateManager/index`，删除不存在的 `/pages/Map/index`，替换为 `/pages/Calendar/index` ("现金流日历")
   - 版本号美化：`version-wrap` 居中容器，版本文字用胶囊样式（圆角背景 + 字间距）

- 同时确认：AddTransaction/Profile/Statistics/Home/Transactions 五个 custom nav 页面均已正确处理 safe area paddingTop

## 导航栏统一（用户反馈 — 五页面标题对齐+高度）

用户截图五个一级界面，指出顶部标题左右不一、高度不一致。
- **方案选定**：全部居中（方案1）
- **改动**：
  - Home: `home-nav-row` 加 `justify-content: center`，标题居中
  - Statistics: `stats-nav-row` 加 `justify-content: center`，标题居中
  - Transactions: 标题居中 + 搜索栏从导航行移到独立的 `txns-search-bar` 区域（导航栏不再拥挤）
- Profile / AddTransaction 已居中，无需改动
- 编译通过

## 建议1/3/4 — Icon 统一 + 可收起搜索

用户选定实施建议 1（搜索可收起）、3（全局 icon 统一）、4（TabBar 图文替换）。

### Icon 组件增强
- 已有 `src/components/Icon/` 组件（CSS 绘制的几何线型图标系统，19 种图标）
- 新增 `template` 图标（页面+横线样式），总图标数 20 种
- 图标系统优势：无外部依赖，CSS border 绘制，完美兼容小程序，颜色通过 prop 控制跟随主题

### TabBar 图标替换
- emoji（🏠📋📊👤）→ Icon 组件（home/transactions/statistics/profile）
- 选中态: `color='#5B9A7A'` (sage green)，未选中: `color='#B0ADA6'`
- `TabItem.icon` 类型从 `string` 改为 `IconName`

### Profile 菜单图标替换
- emoji（📒💰📂📋📅）→ Icon 组件（book/budget/category/template/calendar）
- 菜单项新增 `IconName` 类型约束

### Transactions 搜索可收起
- 导航栏右侧新增搜索切换按钮（🔍/✕ Icon 切换）
- 默认搜索栏隐藏，点击展开（带动画 fade-in）
- 清空搜索时自动收起搜索栏
- 导航栏布局：左侧 64rpx 占位 + 居中标题 + 右侧 64rpx 搜索按钮 → 视觉平衡

## 导航栏高度统一 + 建议6/7/8

用户截图指出五个一级页面导航栏高度不一致。

### 根因分析
- 所有 nav-row padding 都是 `var(--spacing-sm)` = 16rpx，理论上一致
- 实际差异来自**行内元素高度不同**（单标题 vs 标题+按钮+图标），padding 相同时行高由内容撑开

### 修复：固定 nav-row 高度 88rpx
- `app.scss` `.nav-header`: 设 `height: 88rpx; padding: 0 var(--spacing-md)`
- Home/Transactions/Statistics: 统一 `height: 88rpx; padding: 0 var(--spacing-md)`
- 去掉旧的 `padding: var(--spacing-sm) var(--spacing-md)`（上下由 height 控制）
- 五个页面导航栏高度完全一致

### 建议6: Transactions 长按菜单
- `TransactionItem` 新增 `onLongPress` prop
- Transactions 页面：长按 → `Taro.showActionSheet({ itemList: ['编辑', '删除'] })`
- 编辑 → 跳转编辑页，删除 → 弹出 ConfirmDialog

### 建议7: 数字键盘触觉反馈
- `NumberPad` 组件：所有按键点击 → `Taro.vibrateShort({ type: 'light' })`
- 确认键 → `Taro.vibrateShort({ type: 'medium' })`
- catch 异常防止不支持的设备崩溃

### 建议8: 下拉刷新动画
- 新建 `PullRefresh` 组件：3 颗彩色圆点弹跳动画（Sage+Terracotta+Gold）
- 动画：`coin-bounce` 逐帧延迟 0.15s，上下弹跳 + 缩放
- 文字提示 "拉取账本数据…" / "拉取流水数据…"
- 已集成到 Home 和 Transactions 的骨架屏加载状态中

## Taro 端全面重构（用户要求：公共组件 + 模块拆分 + hooks + 写入记忆）

### 分析结果
- 检查了全部 13 个页面 + 13 个组件 + 3 个 hooks + 6 个 service + 2 个 utils
- 发现 5 类重复模式：容器模板、日期逻辑、分类映射、内联卡片渲染、删除确认

### P0 — 减少重复（已完成）

1. **PageLayout 组件** `components/PageLayout/`
   - 统一 NavHeader + 内容区 + TabBar + skeleton loading
   - Props: `title`, `tabBar?`, `rightContent?`, `leftContent?`, `loading?`, `loadingFallback?`
   - 影响：Home/Transactions/Statistics/Profile 四个 TabBar 页面

2. **useMonthSelector hook** `hooks/useMonthSelector.ts`
   - 返回: `{ year, month, setYear, setMonth, dateRange, monthKey }`
   - 影响：Home/Transactions/Statistics/Budgets/Calendar 五个页面
   - 每个页面减少 ~12 行重复代码

3. **useCategoryLookup 统一使用**
   - Home/Transactions/Calendar 改为使用 `useCategoryLookup()` 而非手动 cmap
   - 每个页面减少 ~7 行

4. **utils/format.ts** 统一格式化
   - `fmtAmount()` / `fmtDate()` / `fmtFriendlyDate()` 三个函数

### P1 — 拆分过长组件（已完成）

5. **BookCard 组件** `pages/Books/components/BookCard/`
   - Books 页面 242 → 110 行（-55%）
   - 自带 renaming/members/invite 三个局部状态
   - Props: `book, isActive, isDefault, onSwitch, onRename, onDelete, onLeave`

6. **清理旧 FilterBar**
   - 删除 `pages/Transactions/components/FilterBar/`（189 行，旧风格 inline styles，未被使用）

### P2 — 记忆规范写入（已完成）

7. **MEMORY.md 更新**：
   - 组件归属：补充 PageLayout, NavHeader, PullRefresh, ProgressBar
   - 导航栏：统一使用 NavHeader 公共组件，禁止手写 nav HTML/CSS
   - 代码组织：文件限制从 300→200 行，150→150 行 SCSS
   - 新增 8a 节：Hooks 拆分原则（跨页面重复逻辑必须封装）
   - 新增强制使用的公共模块清单

### 效果
| 指标 | 优化前 | 优化后 |
|------|:--:|:--:|
| 重复 useState/useMemo 逻辑 | 5 处相同 dateRange | 1 个 hook |
| 单文件最长行数 | 259 | BookCard 118, Books 110 |
| 容器模板重复 | 4 页手写 | 1 个 PageLayout |
| 死代码 | FilterBar 189 行 | 已删除 |

## NavHeader 公共组件封装（用户反馈 — 导航栏结构不统一）

用户截图五个页面指出导航栏结构不一致（有的有左按钮，有的没有，有的有右按钮）
- 新建 `src/components/NavHeader/` 组件

### 组件设计
```tsx
interface NavHeaderProps {
  title: string;           // 标题，始终居中
  leftContent?: ReactNode; // 左插槽（80rpx 宽）
  rightContent?: ReactNode;// 右插槽（80rpx 宽）
}
```
- 三栏布局：左(80rpx) | 中(title, flex-1, 居中) | 右(80rpx)
- 固定高度 88rpx，自动 safe area paddingTop
- 左右插槽为空时自动留白保持标题居中

### 五个页面迁移

| 页面 | leftContent | title | rightContent |
|------|:--:|------|:--:|
| Home | — | 家庭记账 | — |
| Transactions | — | 流水 | 搜索图标 |
| Statistics | — | 统计 | — |
| Profile | — | 我的 | — |
| AddTransaction | ✕ 关闭 | 记一笔/编辑记录 | 模板(新建时) |

### 清理
- 移除 `app.scss` 中旧的 `.nav-header`/`.nav-title`/`.nav-back` 全局样式
- 移除 Home/Transactions/Statistics 页面级 nav SCSS
- 移除所有页面中不再使用的 `statusBarH` 变量
- 五个页面导航栏结构完全统一，高度严格一致

## PageLayout 样式修复（用户反馈）

PageLayout 封装后两个 UI 问题：
1. **Home 余额卡片 label 跑偏** — `className` 错放在外层 View 而非内容区
2. **Profile 用户卡片布局丢失** — SCSS 嵌套在 `.profile-page` 下，PageLayout 未传递该 class

### 修复

**PageLayout 升级**：
- 拆分 `className`（外层容器）和 `contentClassName`（内滚动区）
- 新增 `onScrollToLower` / `lowerThreshold` 支持分页加载

| 页面 | 问题 | 修复 |
|------|------|------|
| Home | `className` 放错位置 | → `contentClassName`，label 加 `display:block` |
| Profile | `.profile-page` 嵌套失效 | → `className="profile-page"` |
| Statistics | 双重 ScrollView | → 移除内层 ScrollView |
| Transactions | 双重 ScrollView | → 移除，`onScrollToLower` 提升到 PageLayout |

确认 AddTransaction/Books/Budgets/Calendar/Categories/TemplateManager 未受影响
