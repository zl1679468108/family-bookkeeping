# 2026-06-02 工作日志

## P2 PRD 撰写完成

通过软件公司团队工作流（许清楚 / software-product-manager）完成两份需求文档：

### P2-1 收据上传
- 存储方案：Supabase Storage，新建 `receipts` bucket（public），免费 1GB
- 不建新表，复用 `transactions.image_url`（存相对路径）
- 路径规范：`receipts/{user_id}/{year}/{month}/{uuid}.jpg`
- API: `POST/DELETE /api/transactions/:id/receipt`（复用 transactions 模块）
- 前端压缩 ≤300KB 后上传，单笔仅一张收据
- 交互：新增/编辑交易时可上传、查看、替换、删除收据

### P2-2 年度报告
- 不建新表，纯聚合查询（PostgreSQL GROUP BY + 窗口函数）
- 新建 `reports` NestJS 模块，单一聚合接口 `GET /api/reports/annual`
- 报告板块：封面 → 总览 → 月度趋势(ECharts) → Top5分类 → 消费之最 → 账本视角 → 成员视角 → 趣味彩蛋 → 结尾
- 前端新增 `html2canvas` 依赖，固定 375px 宽渲染，scale=2 截图
- Open Questions: 趣味彩蛋参考物、年份对比模式

### 技术边界总结
| P2-1 | P2-2 |
|------|------|
| 不建表，不建模块 | 不建表，建 reports 模块 |
| Supabase Storage bucket | 纯 PostgreSQL 聚合 |
| 前端压缩 + 直传 | html2canvas 长图 |

详见 `docs/04-项目需求.md` P2 部分。
