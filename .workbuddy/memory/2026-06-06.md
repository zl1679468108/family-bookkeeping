# 2026-06-06 工作日志

## PC 端加载态统一 — 骨架屏替换

全局统一加载状态：所有文字"加载中..."和旋转图标替换为 `Skeleton` 组件骨架屏。

### P0 — 空白加载态（5处）✅
- **App.tsx PrivateRoute** (2处): 合并为统一布局骨架屏（侧边栏 + 3卡片 + 5行列表）
- **App.tsx 无token加载** (1处): 同上布局骨架屏
- **App.tsx Auth Suspense** (1处): 替换为卡片式骨架屏
- **Books/index.tsx**: 空div → 4个账本卡片骨架屏
- **AddTransaction/index.tsx**: 空div → 表单骨架屏（类型/金额/分类/日期/备注/位置/按钮）
- **TransactionHistoryModal**: 空div → 5行交易列表骨架屏

### P1 — 文字加载替换（6处）✅
- **AnnualReport**: "加载中..." → 封面+总览+图表+列表骨架屏
- **Calendar** (2处): "加载中…" → 日历网格骨架屏 + 交易列表骨架屏
- **TemplateManager**: "加载中..." → 3个模板卡片骨架屏
- **MemberComparison**: "加载中..." → 图表+成员列表骨架屏
- **MerchantList**: spinner+"加载商户数据中..." → 5个商户列表骨架屏

### P2+P3 — 旋转图标 + 缺失加载态 ✅
- **Categories**: 旋转图标 → 6个分类卡片网格骨架屏
- **Budgets**: 新增预算表骨架屏（5行，含分类/金额/支出/进度列）
- **layout.scss**: 移除重复的 `@keyframes skeletonShimmer`

### 编译验证
- TypeScript `--noEmit` 通过，0 errors
