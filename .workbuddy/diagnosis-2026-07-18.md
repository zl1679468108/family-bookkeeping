# 生产地址打不开 — 诊断报告（2026-07-18 22:44）

## 现象
- 前端生产地址 `https://family-bookkeeping-d7c9caa78340e-1305761531.tcloudbaseapp.com/` → 返回 **HTTP 418**（空响应体）
- 后端健康检查 `https://family-bookkeeping-api-prod-259958-6-1305761531.sh.run.tcloudbase.com/health` → 返回 **HTTP 503**，body：
  ```json
  {"code":"SERVICE_FORBIDDEN","message":"Your server is isolated."}
  ```
- 前后端**同时**不可达。

## 结论
**不是代码 / 部署问题，是 CloudBase 环境被隔离。**

## 根因（CLI 实测）
环境为 **体验版（免费）**，每月包内 3000 资源点，当前用量：

| 模块 | 资源点消耗 |
|---|---|
| **Cloud hosting（云托管 = 后端容器）** | **3064.53**（包内 2989 + 按量 75） |
| 静态网站托管（前端） | 8.13 |
| 其他（DB / 存储 / API 等） | < 3 |

- 包内资源点：`3000.00 used / 3000.00 total`（**100% 耗尽**）
- 后端 NestJS 容器 24/7 常驻运行，单月烧掉 >3000 点，把整环境额度吃光
- CloudBase 在配额耗尽后**隔离（isolated）**环境 → 前端连带返回 418、后端返回 503

> 12:48 部署时一切正常，本次为额度耗尽被动触发。

## 恢复步骤（需用户在腾讯云控制台操作，AI 无法代付）
1. **充值 / 购资源包**：CloudBase 控制台 → 环境 `family-bookkeeping-d7c9caa78340e` → 充值或购买资源包
2. **或升级付费版**：标准版资源点更高、容器可常驻，能根治此问题
3. **或等待新计费周期**：当前周期 `2026-06-19 ~ 2026-07-18`，今天 7/18 恰好是最后一天，7/19 起 3000 点重置。
   - ⚠️ 但若按量付费部分已产生欠费，需先结清欠费才会解隔离，否则即便新周期开始仍可能打不开。

## 根治建议
- 体验版 3000 点/月**根本撑不住常驻后端容器**，生产环境应升级付费版。
- 前端静态托管几乎不耗点（~8 点/月），问题 100% 出在后端容器常驻。
- 若暂不升级，可评估后端改为按需起停 / 更轻量的部署形态，降低常驻消耗。

## 事后验证（恢复后）
- 后端：`curl https://family-bookkeeping-api-prod-259958-6-1305761531.sh.run.tcloudbase.com/health` 应返回 `200 {"status":"ok"}`
- 前端：浏览器打开根域名应返回 200、标题「静记」、加载 `/static/js/main.*.js`
